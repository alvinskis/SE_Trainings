import { AlphabetCounterPipe } from './alphabet-counter.pipe';

describe('AlphabetCounterPipe', () => {
  it('create an instance', () => {
    const pipe = new AlphabetCounterPipe();
    expect(pipe).toBeTruthy();
  });
});
